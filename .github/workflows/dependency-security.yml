name: Dependency Security

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pip-audit:
    name: pip-audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install pip-audit
        run: |
          python -m pip install --upgrade pip
          python -m pip install pip-audit

      - name: Audit dependencies
        run: |
          # Temporary allowlist: no fix version currently available for diskcache CVE.
          # Keep this narrow so any new CVEs still fail the workflow.
          pip-audit -r requirements.txt --ignore-vuln CVE-2025-69872
