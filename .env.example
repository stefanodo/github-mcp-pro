GITHUB_TOKEN=ghp_replace_with_real_token

# Optional: protect /mcp with Bearer token auth.
# For public deployments, set both values.
MCP_AUTH_TOKEN=replace_with_long_random_secret
REQUIRE_MCP_AUTH=true
